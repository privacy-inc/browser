extension Allowed {
    enum Subdomain: String {
        case
        platform,
        interactive,
        tr,
        accounts,
        tarifvergleich,
        mobileads,
        tracking,
        cookies,
        widgets,
        analytics,
        dsl,
        public_api = "public-api",
        r_login = "r-login"
    }
}
