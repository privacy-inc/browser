extension Allowed {
    enum Path: String {
        case
        pagead,
        recaptcha,
        swg,
        account,
        ads,
        tr,
        plugins,
        subscription_offer = "subscription-offer",
        ct_html = "ct.html"
    }
}
